{% extends "WCPC2K18Bundle:Base:layout.html.twig" %}

{% block content %}
<div class="row">
    <div class="col-lg-12">
        <div class="card mt-3">
            <div class="card-header">
                Détail des rencontres
            </div>
            <div class="card-body">
                <ul class="list-group">
                    {% for game in games %}
                    <li class="list-group-item">
                        <div class="row">
                            <div class="col">Match {{ game.id }}</div>
                            <div class="col">{{ game.kickoff | date('d/m/Y H:i') }}</div>
                            <div class="col text-right">{{ game.homeTeam.name }}</div>
                            <div class="col text-center"> - </div>
                            <div class="col text-left">{{ game.awayteam.name}}</div>
                            <div class="col"></div>
                        </div>
                    </li>
                    {% endfor %}
                </ul>
                
                {#<table class="table table-striped table-sm">
                    <thead class="">
                        <tr>
                            <th>#</th>
                            <th>Date</th>
                            <th>Equipe 1</th>
                        </tr> 
                   </thead>
                    <tbody>
                        {% for game in games %}
                        <tr>
                            <td>{{ game.id }}</td>
                            <td> {{ game.kickoff | date('d/m/Y H:i') }}</td>
                            <td>
                        </tr>
                        {% endfor %}
                        
                    </tbody>
                </table>#}
            </div>
        </div>
    </div>
</div>
{% endblock content %}
